# Option A (recommended, no CORS): proxy API through main domain nginx → /api
VITE_API_URL=/api

# Option B (subdomain API): requires CORS_ORIGINS on backend
# VITE_API_URL=https://api.sr-tradelink.com
